# LaTeX build artifacts (auxiliary files - regenerated on each build)
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb

# PDF di output del documento principale (rigenerato da latexmk)
/main.pdf

# I PDF di guida UniBO e il frontespizio compilato vengono invece
# committati (sono materiale di riferimento, non build artifacts).
# Vedi: guide-unibo-scrittura-tesi/

# Zip sorgente del frontespizio (i file estratti sono gia' presenti)
frontespizio_unibo.zip

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
